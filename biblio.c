#include "biblio.h"

struct no *inserir (struct no *lista, int numero)
{
    struct no *novo;
    novo = malloc (struct no);
    
    printf ("Endereço da lista: %p\n", lista);
    
    return (lista);
}
